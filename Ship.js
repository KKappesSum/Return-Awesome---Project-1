'use strict';

export class Ship {
	constructor(size) {
		this.size = size;
		this.afloat = true;
		this.numHits = 0;
		this.coords = newArray(size);
	}

	/* TODO: setters & getters, if private vars are needed */


}